

Everything else is just output as text, unless escaped with a tick
which is treated as javascript, but excuted inline

This lets you describe output based on the modules in the system:

        /* Unique resource */
	    {
			.start_resource = 0,
			.num_resource = 23,
			.type = RESASG_UTYPE (Random Id,
					RESASG_SUBTYPE_UDMAsubtyoe_TX_CHAN),
			.host_id = hgsdbnc,
	    }

        /* sdkjdhjnkcmfv */
	    {
			.start_resource = 0,
			.num_resource = 454,
			.type = RESASG_UTYPE (hsdbdnc m,
					sdjgjbhcn ),
			.host_id = khsjdm,
	    }

        /* kjsrrgnbmdfb */
	    {
			.start_resource = 0,
			.num_resource = 4543,
			.type = RESASG_UTYPE (jfgbmv,
					jutuothjmrtgu),
			.host_id = tjgjnb mv,
	    }
